// @shim: legacy re-export — `/templates` 는 `/create/concept` 와 동일한 화면을 쓴다.
// 실제 구현은 src/features/concept/ui/pc/DesktopConceptSelectionScreen.tsx.
export { default } from '@/src/features/concept/ui/pc/DesktopConceptSelectionScreen';
